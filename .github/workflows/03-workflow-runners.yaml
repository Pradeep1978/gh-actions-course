name: 03 - workflow runners

on: push

jobs:
  ubuntu-echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show OS
        run: |
          echo "This job is running on an ubuntuHello from Ubuntu runner"
          echo "Runner OS: $RUNNER_OS"