name: 05 - Event Filters - Activity Type

on:
    pull_request:
        types: [opened, synchronize]
        branches: [main] 

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Running whenever a PR is opened or synchronized and base branch is main.